# Dependencies
/node_modules
/.pnp
.pnp.js

# Next.js
/.next/
/out/
/build
next-env.d.ts

# Production
/dist

# Environment & secrets — NEVER commit (.env.example is the safe template)
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# Local file uploads (STORAGE_PROVIDER=local)
/uploads

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
*.pem
.idea
.vscode/*
!.vscode/extensions.json

# Testing / misc
/coverage

# Nested scaffold project — tracked in its own repo
/openroom

# TypeScript incremental build cache
*.tsbuildinfo
